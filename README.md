# ServiceRoute Integration Module

Система управления заявками на обслуживание с интеграцией в 1С:Бухгалтерия.

## 📋 Описание

ServiceRoute Integration Module — это Spring Boot приложение для управления заявками на обслуживание, которое обеспечивает двустороннюю интеграцию с системой 1С:Бухгалтерия. Система позволяет:

- Управлять заявками на обслуживание (создание, назначение, выполнение)
- Резервировать и использовать запчасти
- Синхронизировать справочники номенклатуры и контрагентов с 1С
- Отправлять данные о выполненных заказах в 1С для создания актов выполненных работ
- Выбирать метод оплаты (наличными, по карте, переводом)

## 🛠 Технологии

- **Java 17**
- **Spring Boot 3.2.0**
- **Spring Security** — аутентификация и авторизация
- **Spring Data JPA** — работа с базой данных
- **H2 Database** — встроенная база данных (файловая, персистентная)
- **Thymeleaf** — шаблонизатор для веб-интерфейса
- **Maven** — управление зависимостями
- **WireMock** — мокирование API 1С для тестирования

## 📦 Требования

- Java 17 или выше
- Maven 3.6+
- Docker (опционально, для WireMock)

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd course_project
```

### 2. Настройка конфигурации

Отредактируйте файл `src/main/resources/application.properties`:

```properties
# Порт приложения
server.port=8887

# URL API 1С
integration.api.base-url=http://localhost:8080/buh/hs/integration/v1
integration.api.key=SECRET-API-KEY-FOR-SERVICEROUTE
```

### 3. Запуск WireMock (для тестирования)

Для локального тестирования интеграции с 1С можно использовать WireMock:

```bash
cd wiremockDocker
./start.sh
```

Или вручную:

```bash
docker run -it --rm -p 8080:8080 --name wiremock \
  -v $PWD/mappings:/home/wiremock \
  --env WIREMOCK_OPTIONS="--global-response-templating" \
  wiremock/wiremock:3.13.2
```

### 4. Сборка и запуск приложения

```bash
# Сборка проекта
mvn clean package

# Запуск приложения
mvn spring-boot:run
```

Или запуск из IDE:
- Откройте проект в IntelliJ IDEA или Eclipse
- Запустите класс `ru.flish1.ServiceRouteApplication`

### 5. Доступ к приложению

После запуска приложение будет доступно по адресу:
- **Веб-интерфейс**: http://localhost:8887
- **H2 Console**: http://localhost:8887/h2-console
  - JDBC URL: `jdbc:h2:file:./data/serviceRouteDB`
  - Username: `sa`
  - Password: (пусто)

## 👤 Учетные записи по умолчанию

При первом запуске автоматически создаются следующие пользователи:

| Логин | Пароль | Роль | Описание |
|-------|--------|------|----------|
| `admin` | `admin` | ADMIN | Администратор системы |
| `engineer1` | `engineer1` | ENGINEER | Инженер |

## 🔐 Роли пользователей

- **ADMIN** — полный доступ ко всем функциям системы
- **MANAGER** — управление заявками (назначение инженеров, отмена заявок)
- **ENGINEER** — работа с заявками (начало работы, резервирование запчастей, завершение заявок)

## 📁 Структура проекта

```
course_project/
├── src/
│   ├── main/
│   │   ├── java/ru/flish1/
│   │   │   ├── client/          # Клиент для работы с API 1С
│   │   │   ├── config/          # Конфигурация (Security, DataInitializer)
│   │   │   ├── controller/      # Контроллеры (Web, API)
│   │   │   ├── dto/             # Data Transfer Objects
│   │   │   ├── entity/          # JPA сущности
│   │   │   ├── repository/      # JPA репозитории
│   │   │   ├── service/         # Бизнес-логика
│   │   │   └── ServiceRouteApplication.java
│   │   └── resources/
│   │       ├── application.properties
│   │       └── templates/       # Thymeleaf шаблоны
│   └── test/
├── wiremockDocker/              # Конфигурация WireMock для тестирования
│   ├── mappings/                # Моки API 1С
│   └── rest_1c.yaml            # OpenAPI спецификация API 1С
├── data/                        # База данных H2 (создается автоматически)
├── pom.xml
└── README.md
```

## 🔄 Основные функции

### Управление заявками

1. **Создание заявки** — создание новой заявки на обслуживание
2. **Назначение инженера** — назначение заявки инженеру (ADMIN)
3. **Начало работы** — перевод заявки в статус "В работе" (ENGINEER)
4. **Резервирование запчастей** — резервирование запчастей для заявки (ENGINEER)
5. **Завершение заявки** — завершение заявки с отправкой данных в 1С (ENGINEER)
6. **Отмена заявки** — отмена заявки (ADMIN)

### Статусы заявок

- `new` — новая заявка
- `assigned` — назначена инженеру
- `in_progress` — в работе
- `completed` — завершена
- `canceled` — отменена

### Интеграция с 1С

#### Получение справочных данных

- **GET /nomenclature** — получение списка номенклатуры (услуги, материалы)
- Фильтрация по типу (`service`, `material`)
- Поиск по наименованию или артикулу

#### Отправка данных о заказах

- **POST /completed-orders** — создание акта выполненных работ в 1С
- Передача данных о выполненных услугах и использованных материалах
- Выбор метода оплаты (наличными, по карте, переводом)

## 📡 API интеграции с 1С

### Схема данных

#### CompletedOrderPayload

```json
{
  "sourceOrderId": "SR-123",
  "completionDate": "2024-01-15T10:30:00",
  "customerTaxId": "79001234567",
  "paymentMethod": "card",
  "services": [
    {
      "nomenclatureId": "12345",
      "quantity": 2.0,
      "pricePerUnit": 1500.0,
      "totalPrice": 3000.0
    }
  ],
  "materials": [
    {
      "nomenclatureId": "67890",
      "quantity": 1.0,
      "pricePerUnit": 500.0,
      "totalPrice": 500.0
    }
  ]
}
```

#### Методы оплаты

- `cash` — наличными
- `card` — по карте
- `transfer` — переводом

### OpenAPI спецификация

Полная спецификация API 1С находится в файле `wiremockDocker/rest_1c.yaml`.

## 🧪 Тестирование

### Запуск WireMock

WireMock используется для мокирования API 1С при разработке и тестировании:

```bash
cd wiremockDocker
./start.sh
```

WireMock будет доступен на порту 8080 и будет использовать моки из директории `mappings/`.

### Тестовые данные

При первом запуске приложения автоматически создаются:
- Роли: ADMIN, ENGINEER
- Пользователи: admin, engineer1

## ⚙️ Конфигурация

### База данных

Приложение использует H2 Database в файловом режиме. База данных сохраняется в директории `data/serviceRouteDB.mv.db`.

Для сброса базы данных удалите файлы в директории `data/` и перезапустите приложение.

### API 1С

Настройки подключения к API 1С находятся в `application.properties`:

```properties
integration.api.base-url=http://localhost:8080/buh/hs/integration/v1
integration.api.key=SECRET-API-KEY-FOR-SERVICEROUTE
```

## 📝 Основные сущности

- **Request** — заявка на обслуживание
- **Customer** — клиент
- **User** — пользователь системы
- **ServiceEntity** — услуга
- **Part** — запчасть/материал
- **ReservePart** — резервирование запчасти
- **PartType** — тип запчасти

## 🔧 Разработка

### Добавление новой функциональности

1. Создайте entity в пакете `ru.flish1.entity`
2. Создайте repository в пакете `ru.flish1.repository`
3. Реализуйте бизнес-логику в сервисе (`ru.flish1.service`)
4. Создайте контроллер для обработки запросов
5. Добавьте шаблоны Thymeleaf при необходимости

### Логирование

Приложение использует SLF4J для логирования. Логи выводятся в консоль с уровнем INFO по умолчанию.
